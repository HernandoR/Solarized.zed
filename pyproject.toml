[project]
name = "solarized-zed-theme"
version = "0.10.0"
description = "Generator for the Solarized theme extension for Zed"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "coloraide>=8.10",
]

[dependency-groups]
dev = ["ruff>=0.6"]
